package com.abueltaweel.presentation.screen.prayers

import androidx.compose.ui.graphics.Color

object MosqueColors {
    val Creamy   = Color(0xFFFFF5E6C0) // خلفية كريمية فاتحة
    val Brown    = Color(0xFFFFF5ECD8) // بني بدل البني الداكن
    val Gold     = Color(0xFFC9A84C)   // ذهبي — ثابت
    val DarkBg   = Color(0xFFFFF5F0E8) // خلفية بيج بدل الداكنة
    val Border   = Color(0xFFC9A84C)   // حدود ذهبية
    val LedRed   = Color(0xFFFF2200)   // حمراء — ثابتة LED أرقام
    val LedGreen = Color(0xFF00FF44)   // خضراء — ثابتة LED أرقام
    val LedBg    = Color(0xFF0A0A0A)   // داكنة — ثابتة LED خلفية
}
